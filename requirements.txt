# API
fastapi>=0.116,<1
uvicorn[standard]>=0.35,<1
pydantic>=2.11,<3
pydantic-settings>=2.10,<3
python-dotenv>=1.1,<2
python-multipart>=0.0.20,<1
httpx>=0.28,<1

# AI and prompt observability
langfuse>=4,<5
openai>=1.109,<2

# Data and infrastructure
# SQLite (sqlite3, stdlib) is the default local/test backend used by every
# raw-SQL store in this repo. psycopg is the optional Postgres backend,
# enabled by setting DATABASE_URL to a postgres://... URL -- see
# docs/postgres-migration.md. sqlalchemy/asyncpg/alembic/redis below are
# unused scaffolding from the initial setup; left in place since nothing
# currently depends on removing them.
psycopg[binary,pool]>=3.2,<4
sqlalchemy[asyncio]>=2.0,<3
asyncpg>=0.30,<1
alembic>=1.16,<2
redis>=6,<7

# Security and operations
PyJWT>=2.10,<3
bcrypt>=4.1,<5
email-validator>=2.2,<3
structlog>=25,<26
sentry-sdk[fastapi]>=2.33,<3

# Test and quality
pytest>=8.4,<9
pytest-asyncio>=1.0,<2
pytest-cov>=6.2,<7
ruff>=0.12,<1
mypy>=1.16,<2
